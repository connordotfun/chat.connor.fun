{
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix ./frontend && npm install eslint-plugin-import --prefix ./frontend && npm run build --prefix ./frontend"
  }
}
